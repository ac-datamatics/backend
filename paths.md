# Paths where audio and video files will be saved

## Unmerged screen recording

Path (bucket to be determined):

BUCKET_NAME/screen-recordings/`year`/`month`/`day`/`contactId`_`year` `month` `day`T`hours`:`minutes`_UTC.webm
